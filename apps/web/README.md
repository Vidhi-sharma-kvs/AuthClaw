# Web Console

The AuthClaw web console runtime lives in:

- `../../frontend/`

It contains the React/Vite dashboard for onboarding, gateway chat, requests,
approvals, audit logs, providers, API keys, policies, and settings.
